namespace IceCity
{
    // WHY CHANGED: Removed unnecessary "using" directives. An enum needs no imports.
    // Having unused usings is noise — they mislead developers into thinking those
    // namespaces are needed and slow down compilation slightly.
    public enum HeaterType
    {
        Gas,
        Electric,
    }
}
